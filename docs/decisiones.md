# Decisiones

Una línea por decisión, con la alternativa descartada y el motivo.
Formato: `- [quién] decisión — alternativa descartada y por qué`.

## Dirección visual

- [ux-lead] La dirección de arte sale de las fotos reales de `docs/plants/` (etiqueta térmica de Viveros Projardín, Móstoles) — descartado el registro "herbario botánico del XIX", que es bonito pero es de otra casa: estas plantas costaron 2,25 € y vienen con código fitosanitario, no con una lámina de Kew.
- [ux-lead] Fondo de página en teja `#8F4A33`, el plástico del tiesto — descartado el fondo crema `#F4F1EA` (cliché nº 1 del brief) y el casi negro (cliché nº 2); descartado también el blanco, que habría dejado las fichas sin borde perceptible y sin identidad.
- [ux-lead] Verde botella `#16342A` como **tinta**, nunca como fondo — descartado el verde salvia sobre blanco, que es exactamente el cliché de dominio que prohíbe el brief; usarlo como tinta lo convierte en decisión en vez de en decorado.
- [ux-lead] `severidad: sana` no tiene color propio, se pinta en Tinta — descartado el semáforo verde/ámbar/rojo, que habría gastado un color en la información menos urgente de la página; el color solo aparece cuando hay un problema.
- [ux-lead] Azul `#1F3F97` (el del `PLANT PASSPORT` de la begonia) reservado en exclusiva a la capa personal de Carlos y a las fuentes — descartado usarlo como acento general, que habría borrado la única señal cromática que separa las dos capas de contenido.
- [ux-lead] La separación entre capa dura y capa de Carlos usa cuatro señales simultáneas (superficie cuadriculada, itálica, tinta azul, rótulo `CARLOS`) — descartado separarlas solo por color, que incumple "ninguna información puede depender solo del color".
- [ux-lead] Las fuentes citadas van al pie de cada campo con el dominio visible y a `--texto-xs` — descartado el bloque de bibliografía al final de la ficha, que las convierte en letra pequeña cuando son parte del valor.

## Tipografía

- [ux-lead] `Archivo Narrow` 700 para el display, con fallback real `Arial Narrow` — descartadas `Oswald` (marcada como Google Fonts de 2015) y `Bebas Neue` (cartel, no etiqueta); Archivo Narrow es una grotesca de impresión y es el registro literal de la etiquetadora del vivero.
- [ux-lead] `Alegreya Sans` para el cuerpo, y su itálica como voz de Carlos — descartado `Inter` por ser el default absoluto de cualquier web actual, y descartada toda serif de alto contraste por ser la mitad del cliché nº 1.
- [ux-lead] `IBM Plex Mono` para cifras, °C, ml, €, EAN y códigos — descartado `JetBrains Mono` (registro de editor de código) y descartado usar la misma sans para los números, que les quita el efecto de dato medido.
- [ux-lead] Ninguna fuente se carga hoy: las tres pilas declaran el fallback de sistema y los `woff2` quedan como tarea pendiente — descartado enlazar Google Fonts, que rompe la restricción de cero requests a terceros, y descartado bloquear a `builder` hasta tener los ficheros.
- [ux-lead] Base tipográfica de 17 px (`1.0625rem`), no 16 — el texto se lee con prisa y con una mano ocupada; el escalón extra se nota y no cuesta nada.

## Layout e interacción

- [ux-lead] Una sola pantalla: la ficha se abre en su sitio a ancho completo (`grid-column: 1 / -1`) — descartadas la página de detalle y la modal, porque el trabajo primario ("una planta mía tiene un problema ahora") no admite perder el sitio en la lista.
- [ux-lead] La ficha es `<details>` y funciona sin JS; el JS solo añade buscar, filtrar y el plano — descartado renderizar la apertura desde JS, que deja la web inservible si falla un módulo.
- [ux-lead] Sin fotos en la rejilla cerrada: identifica la etiqueta de precio, no una miniatura de hoja — descartada la rejilla de fotos, donde las siete plantas se parecen entre sí y no se encuentra ninguna rápido.
- [ux-lead] Las plantas con `estado != sana` van primeras en el DOM — descartado el orden alfabético; el orden es información y ordenar por nombre esconde justo lo urgente.
- [ux-lead] Container queries para las fichas, media queries solo para decisiones de página — descartado el breakpoint global, que obliga a tocar CSS cada vez que la ficha cambia de hueco.
- [ux-lead] Las pegatinas van rectas — descartada la rotación ligera tipo "sticker pegado a mano", que es simpática una vez, molesta al escanear la rejilla y se lee como truco.

## Signature y SVG

- [ux-lead] Signature: la etiqueta de Projardín reconstruida en HTML/CSS, con el botón "despegar" — descartado apostar la identidad a un SVG bonito de hoja, que es lo que haría cualquier generador; la etiqueta con nº fitosanitario no sale de un prompt genérico.
- [ux-lead] Código de barras con `repeating-linear-gradient` — descartada una imagen o un SVG generado, que añade peso y ficheros para algo que es una trama.
- [ux-lead] Los cinco SVG explican un dato y el dato está también en texto al lado — descartado el diagrama decorativo: si se puede borrar sin perder información, se borra.
- [ux-lead] Cada animación tiene definida su versión reducida, y ninguna arranca sola ni va en bucle — descartado el `@media (prefers-reduced-motion)` que pone todo a `animation: none`, que trata la accesibilidad como un parche final en vez de como una segunda versión diseñada.

## Segunda vuelta — después de los datos reales de `botanist` y de las fotos de `builder`

- [ux-lead] Los 5 SVG se reescriben sobre los ejes que existen de verdad en el JSON (`riego.dias_*` 2–10, `luz.nivel` 1–5, `temperatura.min_c/max_c` 7–30) — descartados los diagramas de centímetros de profundidad, mililitros y metros hasta la ventana: nadie produce esos datos y dibujarlos habría sido precisión fingida.
- [ux-lead] El plano de casa se aparca y su hueco lo ocupa "el calendario del domingo" (las 7 plantas en un eje común de días entre riegos) — descartado mantener el plano con datos inventados; y el calendario resulta responder mejor al trabajo primario ("¿a cuál le toca?") que el propio plano.
- [ux-lead] Cuando falta un dato se dibuja el eje con el hueco en `--trama-sin-dato` y el texto dice "sin dato" — descartado ocultar el diagrama, que hace creer que no había nada que saber, y descartado estimar el valor, que `CLAUDE.md` prohíbe.
- [ux-lead] `--color-sin-dato #63625B`, gris neutro, nunca verde, y explícitamente no es un estado — descartado el semáforo de dos posiciones para toxicidad: `botanist` confirma que **no hay ni una planta "no tóxica" verificada**, así que un icono verde mentiría en cinco de siete fichas.
- [ux-lead] Toxicidad con tres presentaciones distinguibles por borde e icono además de color (`toxica` / `sin_datos_aspca` / `sin_identificar`) — descartado agrupar "sin datos" con "no tóxica", que convierte un hueco documental en una afirmación de seguridad.
- [ux-lead] `critica` se pinta **invertida** (blanco sobre rojo sólido) y `atencion` se queda en texto sobre relleno — descartado usar dos intensidades del mismo tratamiento: con una sola planta crítica y dos en atención, el salto tenía que ser de peso y no de tono, o la crítica no destaca.
- [ux-lead] La fecha de observación va pegada al diagnóstico, no al pie — descartado omitirla: `estado` describe el 11 ago 2026 y sin fecha la ficha estará afirmando algo falso en tres semanas.
- [ux-lead] El panel de cuaderno se renderiza solo si hay contenido — descartado el placeholder "aún no hay notas", que es decorar una ausencia siete veces; `historia` y `notas_carlos` están a `null` en las 7 y eso es una pregunta para Carlos.
- [ux-lead] Las 5 fotos de etiqueta entran en la UI, pero solo en el bloque de procedencia, a `--ancho-prueba` y sin estilizar — descartado dejarlas fuera (son la fuente más fuerte de qué compró Carlos) y descartado subirlas junto a la cabecera, donde la foto real le comería la escena a la etiqueta dibujada.
- [ux-lead] `helecho` y `poto` usan la variante "sin etiqueta de vivero": mismo nombre a sangre, y las barras y el precio sustituidos por trama y una raya `—` — descartado darles una ficha de otra forma, que rompería el sistema justo en las dos plantas que peor se identifican.
- [ux-lead] El nº fitosanitario no se usa como identificador de planta — el ficus y el coleo grande comparten el mismo: es del vivero, no de la planta.
- [ux-lead] "Funciona sin JS" se corrige a "el abrir y cerrar no lleva JS": la ficha se renderiza con `fetch` y sin JS no hay página — la frase original del brief era incorrecta y `builder` hizo bien en pararla; la exigencia que sí queda es que ningún comportamiento nativo se reimplemente en JS.
- [ux-lead] **Las 5 fotos de etiqueta entran en la UI (opción B), pero solo en el bloque de procedencia de la ficha abierta**, a `--ancho-prueba` y sin estilizar — descartada la opción A (dejarlas fuera): para cinco de las siete plantas la pegatina es la fuente más fuerte que existe de qué compró Carlos, más que POWO, que dice qué es una especie pero no qué hay en ese tiesto. El riesgo de que la foto le coma la escena a la etiqueta dibujada se desactiva por separación —cabecera a ancho completo contra 9rem al pie, en otro contexto de lectura— y no por filtros.
- [ux-lead] Los seis `@font-face` van comentados mientras `assets/fonts/` esté vacío — descartado declararlos "por dejarlo listo": seis 404 por carga rompen la condición de consola limpia, y las pilas de `font-family` ya nombran las familias primero, así que descomentar el día que lleguen no toca nada más.
- [ux-lead] La única fuente de verdad de los nombres de token es `css/tokens.css`; el brief es explicación, no contrato — descartado mantener los nombres humanos del brief (Alarma, Bolígrafo) como si fueran normativos: `builder` los habría copiado de la tabla y no habrían existido.
- [team-lead] La teja se queda como fondo y no se invierte con el verde botella — descartado el verde de fondo porque es a la vez el cliché nº 2 (campo oscuro con acentos) y el color que cualquier generador elegiría para "web de plantas".

## Tercera vuelta — auditoría de `qa-visual` y contexto real del salón

- [ux-lead] `--color-sin-dato-trama` sube de `#B9B5AA` (1,72:1) a `#847F70` (3,35:1) — descartado defender que las diagonales son textura decorativa: son el portador **no cromático** de "aquí no hay dato", así que son gráfico informativo y les aplica el 3:1. La auditoría de `qa-visual` tenía razón.
- [ux-lead] `--color-codigo` sube de `#5E7066` a `#4F6157` — descartado dejarlo en 4,54:1 sobre la superficie hundida: pasaba AA por 0,04 y cualquier retoque futuro de cualquiera de los dos colores lo tumbaba. Un margen no es un aprobado.
- [ux-lead] Añadido `--texto-fuente-sobre-maceta` y acotada la regla de enlaces a la ficha y al panel — descartado dejar `a { color: var(--texto-fuente) }` suelto en la capa `base`: sobre el fondo teja da 1,44:1 y es una mina para el primer enlace que alguien meta en el pie.
- [ux-lead] **El plano de casa se cierra definitivamente**: las siete plantas están en el salón — descartado dibujarlo con las habitaciones reales, porque serían siete puntos amontonados en una sola habitación, o sea cero información y por tanto diagrama decorativo.
- [ux-lead] El SVG 2 pasa de "escala de luz de 5 escalones" a **"la mañana del salón"**, un eje de un día con el tramo de sol directo y el de sombra luminosa — descartada la categoría abstracta `luz.nivel` como única fuente: Carlos confirma que por la mañana les da sol directo y luego se va, y esa historia con dos tramos es un dato real que el diagrama puede contar.
- [ux-lead] El SVG 3 recupera los marcadores de casa con números reales (28 °C de tope en verano por el aire acondicionado) — descartado dejarlo como rango botánico abstracto: la utilidad está en ver dónde cae la casa dentro de la banda, no la banda sola.
- [ux-lead] El calendario del domingo deja de ser una comparación entre sitios y pasa a ser **una sola escena de riego** — las siete comparten microclima, lo que refuerza el diagrama en vez de debilitarlo.

## Cuarta vuelta — verificación contra el JSON real

- [ux-lead] **Retiro mi propio SVG 2 del turno anterior**: `plantas.json` afirma ventanal NE y "cero sol directo" planta por planta, en contra de la frase de Carlos que me llegó de segunda mano — descartado elegir la versión que me convenía para el diagrama; el conflicto queda documentado en el brief y lo cierra Carlos mirando por la ventana, no yo deduciéndolo.
- [ux-lead] El SVG 2 pasa a **"lo que quiere y lo que tiene"**, dos marcas sobre la escala 1–5 — descartada la escala de una sola marca: el dato interesante no es en qué escalón vive sino cuánto le falta, que es lo que dicen los propios textos de `botanist` ("quiere más luz de la que tiene"). Requiere partir `luz.nivel` en `nivel_actual` / `nivel_ideal`.
- [ux-lead] **La toxicidad sale del registro de alarma**: se pinta en tinta sobre superficie hundida, con borde sólido e icono, no en rojo — `meta.contexto` dice que no hay mascotas en casa, así que el rojo la habría igualado visualmente al helecho, que sí se está muriendo. En este sistema el color significa "haz algo hoy" y hay una sola planta crítica: gastarlo en un dato informativo se la come.
- [ux-lead] La toxicidad se pinta como **dos campos** (`gatos` y `perros`), no como uno — descartado el badge único: hoy coinciden en las siete, pero el esquema permite que diverjan y un badge que promedia dos animales miente el día que la fuente los separe.
- [ux-lead] `estado.no_visible_en_foto` se muestra **dentro del bloque de diagnóstico**, no plegado en un detalle — es la misma lógica que las fuentes citadas visibles: decir hasta dónde llega lo que sabes es parte de saberlo, y en el helecho ese bloque será más largo que el propio diagnóstico.
- [ux-lead] `riego.ml_aprox` vuelve al texto del SVG 1 — lo había descartado por creer que no existía; `botanist` lo tenía y es el dato que convierte "cada 4 días" en una instrucción ejecutable.

## Quinta vuelta — tipografías reales y la cronología

- [ux-lead] Los seis `woff2` se descargan ya subseteados al rango latino y se sirven desde `assets/fonts/` (82,1 KB) — descartado `fonttools` para subsetear, que sería una dependencia; y descartado dejar la system stack ahora que Carlos ha aprobado el self-hosting. La descarga es de desarrollo, una vez: en runtime no hay ni una petición a terceros.
- [ux-lead] La procedencia y la licencia OFL de las tres familias quedan anotadas dentro de `css/tokens.css` — en un proyecto sin dependencias esos seis ficheros son lo único con licencia de terceros que hay en el repo, y un `LICENSES.md` aparte se separa del sitio donde se lee.
- [ux-lead] El `unicode-range` declarado es exactamente el del fichero descargado, no el que yo había escrito a ojo — declarar un rango más ancho que el real hace que el navegador se descargue la fuente para glifos que no contiene y caiga al fallback igual.
- [ux-lead] **"Recién llegada" NO entra como cuarto estado de severidad** — descartado añadirlo a la escala: severidad es "cómo está" y esto es "desde cuándo", y son ejes independientes (se puede estar sana y recién llegada, o llevar veinte años y estar crítica). Un cuarto escalón que no compite con los otros tres obliga a leer una escala que no existe.
- [ux-lead] "En aclimatación" se calcula de `fecha_llegada` y se muestra tres semanas — descartado el campo de estado explícito: en dos semanas será falso y así desaparece solo, sin tocar diseño ni contenido.
- [ux-lead] **La ausencia de etiqueta de vivero pasa de hueco a antigüedad**: el bloque `LA PRUEBA` dice "sin etiqueta: lleva más de veinte años en la familia" — descartado seguir tratándola como dato que falta; la signature deja de ser recurso estético y pasa a ser cronología, que es sentido gratis.
- [ux-lead] **El patrón de que las dos plantas tocadas son los dos regalos NO se subraya** — descartado el rótulo, la agrupación y la nota al pie: el trabajo del diseño es poner origen y estado en el mismo golpe de vista para que se vea solo; señalarlo con el dedo lo convertiría en una gracia.

## Sexta vuelta — más superficie visual, y el lector resulta ser Noah

- [ux-lead] El tramo de sol vuelve como **segunda fila del mismo SVG 2**, no como diagrama aparte — el JSON estaba caduco, no en desacuerdo: la frase "sin sol directo" era una deducción de la orientación NE, y Carlos describe su propio salón. La escala con el hueco es el dato y el tramo de sol es el porqué; separarlos habría partido en dos una sola explicación.
- [ux-lead] Diagrama 6, **la cronología**, con eje **logarítmico** y marcas rotuladas — descartado el eje lineal, en el que el poto (20 años) ocupa todo el ancho y las otras seis son un borrón; y descartado no rotular las marcas, porque un eje logarítmico sin rótulos miente sobre las proporciones.
- [ux-lead] **Siete siluetas de hoja de línea en la rejilla cerrada** — arregla un agujero que abrí yo: al quitar las fotos, las siete fichas se distinguían solo por el texto del nombre. La silueta es legítima porque la forma de la hoja **es la clave de identificación botánica**, no un adorno vegetal; y el sistema no se importa de fuera, se levanta de la fila de pictogramas que ya trae la etiqueta real de la begonia.
- [ux-lead] La silueta del helecho se dibuja **con trama de sin-dato dentro del contorno** — descartado dibujarle una hoja de *Adiantum* bonita: está sin identificar, y afirmar una especie con un lápiz es la misma mentira que rellenar un campo a ojo.
- [ux-lead] Las siluetas **no se animan** — descartada la entrada escalonada: son el ancla para escanear la rejilla y algo que se mueve no ancla nada. Es el único elemento del sistema sin versión reducida, porque no hay versión.
- [ux-lead] Iconos de campo **junto a la versalita, nunca en su lugar** — descartado el icono solo: obliga a aprender un vocabulario antes de poder usar una web que se abre con prisa una vez cada quince días.
- [ux-lead] El aviso de savia irritante va **dentro del paso de tratamiento** que la provoca, no en el campo de toxicidad — el campo describe la especie; el riesgo real es de manipulación y pertenece a la acción, no a la ficha.
- [ux-lead] **La toxicidad no vuelve al rojo** pese al aviso de que había un niño (Noah tiene 20 años) — se mantiene el principio: el color significa "haz algo hoy", y tres fichas en rojo se comerían al helecho, que es el único que se está muriendo.
- [ux-lead] El panel personal se queda en **una sola superficie con el nombre del autor tomado del dato** (`nota.autor`), no en dos paneles para Carlos y Noah — descartadas dos superficies: el panel codifica un **registro** ("esto no tiene fuente y no la necesita"), no una persona, y dos superficies para el mismo tipo de enunciado obligan a aprender dos cosas para entender una. Además así cabe una tercera voz (Vanesa) sin inventar nada.

- [ux-lead] La tensión grabado del XIX / plástico de vivero se produce por **emplazamiento**, no por textura: dibujo en registro de lámina dentro del troquel de una pegatina que dice `2,25€` — descartado añadir tramas de aguafuerte a las siluetas: a 40 px el rayado de volumen es barro, y con textura de grabado dejaría de ser una etiqueta de vivero con un dibujo serio para convertirse en una web bonita de plantas.

## Séptima vuelta — revisión contra el JSON definitivo de `botanist`

- [ux-lead] Bajo las barras se imprime `CÓD. 2040 2174` y no `EAN`: solo la begonia tiene EAN-13 real (`8437018857012`) — descartado rotular como EAN el código interno del vivero de ocho dígitos. La banda de barras sí se dibuja, porque la pegatina real la lleva impresa; lo que no se puede es llamarla como no es.
- [ux-lead] La celda del precio pasa a ser **celda de procedencia** y cada ficha imprime lo que su etiqueta trae — la begonia **no tiene precio** porque es etiqueta de productor, no de vivero; el coleo grande dice `MACETA 1,6 L` porque da volumen y no diámetro; el pequeño no dice nada de la maceta. Descartado normalizar los cinco a un mismo esqueleto: la pegatina reproduce lo impreso, no lo deseable.
- [ux-lead] En helecho y poto, el renglón de procedencia dice "sin trazabilidad comercial: probablemente un esqueje" y "rescatado de una desecación" — descartado dejarlo como hueco marcado: la observación es de `botanist` y es mejor contenido que la burocracia comercial que ocupa ese sitio en las otras cinco.
- [ux-lead] Las fuentes se citan por **nombre corto** (`RHS ↗`, `POWO ↗`) y no por dominio — se lee mejor y es lo que trae el campo; y cuando la fuente es `Carlos` u `observación de foto` **no se pinta como enlace**, porque no lo es.
- [ux-lead] El pie del diagrama de riego declara que días y mililitros son **estimación para esta maceta**, no cifra publicada por RHS — presentarlos con el sello de RHS atribuiría a una fuente algo que no dice; es la separación capa dura / capa personal aplicada **dentro** de la capa dura.
- [ux-lead] **Fuera del diagrama la marca de "mínima letal"**: `minima_letal_c` es `null` en las siete y RHS publica bandas de rusticidad, que dicen dónde puede vivir una planta y no a qué grado muere — descartado convertir `rusticidad_rhs` en una marca del eje de grados: es una banda climática y ponerla ahí la convierte en otra cosa.
- [ux-lead] El helecho lleva el eje térmico **abierto por la derecha** (`≥ 10 °C`, extremo en trama y sin punta) — `max_c` es `null` y cerrar el eje sería inventar un máximo.
- [ux-lead] El diagrama de luz pasa a tener **signo**: `exceso` / `en su sitio` / `déficit`, con dirección y palabra — descartada la escala plana de "cuánto le falta": el caso más frecuente resultó ser el contrario (tres plantas con **exceso** y riesgo de quemadura) y era el que yo no había previsto. Sin color: exceso y déficit son diagnósticos, no alarmas.
- [ux-lead] El curso de recuperación cambia de eje temporal a **eje de hitos** — `revisar_en` no es fecha ISO sino texto con plazo y criterio, y `plan_recuperacion[]` trae la señal observable de cada paso: lo que deja avanzar no es que pasen siete días, es que veas el brote.
- [ux-lead] Las cuatro plantas sanas muestran su `estado` bajo el rótulo `QUÉ VIGILAR`, misma posición y sin color — descartado no pintarlo: no llevar distintivo no puede significar que su contenido no se vea; una planta sana no necesita alarma, necesita saber qué la va a estropear.
- [ux-lead] El distintivo de `sin_datos` no puede llevar nada legible como aprobación —ni check, ni círculo relleno, ni verde—, borde **discontinuo** e icono `?`, y en los coleos la nota del casi-homónimo — ASPCA tiene ficha de "Prostrate Coleus", que **es otra especie** y sí es no tóxica: si el distintivo se lee como seguro, la web puede contribuir a una urgencia veterinaria.

## Octava vuelta — corregir mirando la página, no el spec

- [ux-lead] **La trama de sin-dato sale de la pegatina de helecho y poto**: no hay banda de barras ni raya de precio, sino el texto de procedencia — descartado marcar el hueco, porque `--trama-sin-dato` significa "no lo sabemos" y que una planta no traiga etiqueta es un **hecho conocido**, no una laguna. Renderizado, el rectángulo tramado se leía como un placeholder de carga. La trama sí se queda dentro del contorno de la silueta del helecho, donde la especie sí es un dato desconocido: esa es la línea entre los dos casos.
- [ux-lead] "(sin identificar)" baja del nombre a sangre al renglón del nombre científico — descartado imprimirlo a cuerpo de titular: el nombre grande existe para encontrar la planta de un vistazo y el estado de identificación no ayuda a encontrarla; además duplicaba la cadena más larga de la página, de 7 a 25 caracteres.
- [ux-lead] `--texto-nombre` baja el suelo del `clamp` de 2.25rem a 2rem y exige `hyphens: auto` con `lang="es"` — a 320 px, con ~224 px útiles, una palabra de 12 letras a 36 px ocupaba el 87 % del renglón y no dejaba margen para ningún nombre más largo. Una condensada de display sin partición es una bomba de desbordamiento.
- [ux-lead] El bloque hundido de resúmenes se estira hasta el pie de la ficha — la ficha más corta dejaba una banda blanca que las vecinas no tenían: una ficha con menos contenido debe verse más vacía por dentro, no rota por fuera.
- [ux-lead] Los cuatro segmentos del pasaporte fitosanitario son unidades indivisibles (`nowrap` por segmento) — un código de trazabilidad partido por la mitad deja de ser un código.
- [builder/ux-lead] La microlínea de la pegatina se lee de `meta.vivero` y no de una constante, así que la begonia imprime `GONZA S.A.T. / PRODUCT FROM ALMERÍA` — decisión de `builder`, validada: es lo que dice su etiqueta real y va en la dirección de la signature más que mi versión.

## Novena vuelta — los dos modos de la página

- [ux-lead] **Mientras hay una ficha desplegada, la rejilla colapsa a una columna** (`.rejilla:has(details[open])`) — descartado `grid-auto-flow: dense`, que rellena el hueco pero **desacopla el orden visual del orden del DOM y con él el de foco**: no se cambia accesibilidad por relleno. Descartado también reducir la ficha abierta a dos columnas de tres (deja hueco igual y le quita sitio al diagnóstico, que es lo que más contenido tiene) y reordenar al desplegar (mueve la ficha que acabas de tocar).
- [ux-lead] El diagnóstico de fondo: **la página hace dos trabajos y yo le había dado una sola forma** — modo estantería (rejilla, para buscar y comparar) y modo ficha (una columna, para leer una). Cambiar de layout entre los dos no es parchear un hueco: es admitir que hay dos trabajos. Y una pegatina ancha se parece más a la etiqueta real de Projardín que una cuadrada, así que no es degradación estética.
- [ux-lead] Las fichas pasan a `<details name="planta">`, acordeón exclusivo **nativo** — una sola abierta a la vez sin JS, garantiza que el modo ficha va siempre de una planta y elimina que dos fichas a ancho completo abran dos huecos.
- [ux-lead] El salto que provoca el colapso se compensa anclando el `<summary>` abierto con `scrollIntoView` + `scroll-margin-block-start` — no es reimplementar algo nativo, es corregir un desplazamiento que provoca nuestro propio cambio de layout.
- [botanist/ux-lead] Los rasgos de las siete siluetas los fija `botanist`: cuatro de mis seis descripciones eran bonitas y erróneas — begonia crenado-serrada y no ondulada; coleo con el par de hojas opuestas sobre tallo cuadrado; *Ficus pumila* festoneada y de nervios hundidos, no "nervio marcado"; poto **juvenil** y no pinnatisecta.
- [ux-lead] La silueta de la margarita se dibuja con lóbulos **gruesos e irregulares**, no finamente dividida — la etiqueta dice MARGARITA y la planta es un crisantemo, y la hoja **es la prueba**: dibujada fina parecería *Argyranthemum*, estrecha y dentada *Leucanthemum*. El dibujo deja de ilustrar la identificación y pasa a ser su evidencia.
- [botanist/ux-lead] Campo nuevo `manipulacion` con icono de guante, al nivel de sustrato o abonado y **sin color** — la toxicidad no vuelve al rojo, pero la vía que sí existe en esta casa es dérmica y ocular, porque quien poda y trasplanta es Noah. Fuente RHS (personas), no ASPCA (veterinaria).
- [ux-lead] `manipulacion` se pinta **también cuando dice que no hace falta** (helecho: tejido seco, sin savia) — inflar la precaución donde no aplica es la forma de que nadie se la crea donde sí. Misma regla que hace que `sana` no lleve distintivo.
- [botanist/ux-lead] La frase de `sin_datos` pasa a la de `botanist`: "No significa que sea segura: **significa que nadie la ha evaluado**" — descartada la mía, que dejaba abierta la lectura de "probablemente no pasa nada".

## Décima vuelta — dos velocidades de lectura y la franja con fecha

- [ux-lead] La ficha abierta se parte en **dos columnas semánticas** —`QUÉ HAGO AHORA` (2fr, sticky) y `EN QUÉ ME BASO` (3fr)— y no en dos columnas de texto — descartado partir el texto para ahorrar scroll: son dos lecturas distintas del mismo contenido (con la regadera en la mano contra el domingo sentado), y yo tenía especificadas dos **capas** pero no dos **velocidades**. Era el eje que faltaba, y resuelve el hueco y la jerarquía a la vez.
- [ux-lead] Los diagramas van en la columna de acción, no en la de evidencia — son la capa de resumen (el SVG dice "cada 4 días, 250 ml" de un golpe y el párrafo explica por qué), y de paso equilibran la masa de las dos columnas.
- [ux-lead] **Nada se pliega**, ni siquiera los patrones de reconocimiento — descartado el progressive disclosure que proponía el lead: la columna **es** la jerarquía, y un `<details>` dentro de otro `<details>` habría sido además un lío de teclado.
- [ux-lead] `PATRÓN PARA RECONOCERLA` pasa de frase en versalitas dentro del párrafo a **elemento propio** con filete izquierdo — no es una afirmación más, es una **instrucción para mirar**, y es lo que convierte un listado de causas en algo usable delante de la planta. Hoy es invisible.
- [ux-lead] **La franja `HOY` no dice nunca "hoy le toca" si nadie ha marcado nunca ese riego** — descartado estimar el vencimiento desde `fecha_llegada`: un contador que se cree más listo de lo que es miente peor que no tener contador, porque un dato ausente se detecta y uno inventado no. Tres niveles de certeza con palabras distintas: fecha dura, ritmo, vencimiento.
- [ux-lead] La memoria vive en `localStorage` y **se dice en voz alta** ("lo que marcas se guarda solo en este navegador", una línea, una vez) — descartado ocultar que no se sincroniza entre el móvil de Noah y el portátil de Carlos: el coste se dice, no se descubre.
- [ux-lead] **Sin notificaciones, sin rachas, sin porcentaje de cumplimiento** — la página se abre con prisa una vez cada quince días; cualquier cosa que premie abrirla más a menudo trabaja contra el uso real.
- [ux-lead] La franja no crece: si hay siete tareas dice el número y las dos primeras, y el resto viven en sus fichas, que es donde se hacen.
- [ux-lead] Nada de cuentas atrás en vivo: se calcula al cargar — un número que se mueve solo es justo lo que `prefers-reduced-motion` existe para apagar, y aquí no aporta nada.
- [botanist/ux-lead] Se cae "probablemente un esqueje" del renglón de procedencia del poto — era una conjetura, y el renglón de la etiqueta es el sitio de la web con más aspecto de dato duro. Poda de `botanist`, aceptada.
- [ux-lead] `(sin identificar)` no puede vivir dentro de `nombre_comun`: es **texto de interfaz metido en un valor de contenido**, y por eso `builder` no podía quitarlo. El estado se renderiza desde `nombre_cientifico: null`.

## Undécima vuelta — el expediente y los cinco tipos de tarea

- [ux-lead] **La metáfora cambia al abrir y se declara**: la ficha cerrada es la pegatina, y debajo no hay el reverso de una pegatina sino **el expediente de la planta** — descartado recortar contenido para que la metáfora encaje en 4.801 px, que sería subordinar el contenido a la ocurrencia. Y no es rendirse: las plantas de vivero tienen expediente de verdad (pasaporte, lote, productor) y la pegatina es su portada.
- [ux-lead] Índice con **recuento** en la columna sticky (`Lo que se ve · 13`, `Fuentes · 24`) — no para esconder sino para entrar; y el número es información: dice que el diagnóstico está trabajado antes de leerlo. Un índice que oculta cuánto documento hay es un índice que engaña.
- [ux-lead] **Se suprime el `resumen` de los campos que en la ficha abierta ya muestran diagrama + `detalle`** — hoy se pinta dos veces la misma frase en la misma pantalla; el resumen existe para la rejilla cerrada, donde es lo único que hay.
- [ux-lead] Sigue sin plegarse nada, y con mejor argumento: con dos columnas y un índice, esconder además sería cobrar dos veces por la misma decisión. La línea: **plegar por longitud es razonable; plegar los límites de lo que sabemos, no** — un índice mueve al lector, un `<details>` sobre "esto no lo podemos afirmar" decide por él que no le interesa.
- [ux-lead] `--color-borde-sobre-maceta` (`#C1BCB3`, 3,48:1) para controles sobre el fondo — `--color-borde` daba 1,92:1 sobre la teja y suspendía WCAG 1.4.11 en el borde del buscador, que es *el* control de la página. Pero la salida preferente es la de `qa-visual`: **poner el buscador sobre una etiqueta blanca**, que además lo convierte en otra pegatina en el tiesto.
- [botanist/ux-lead] **El riego nunca lleva fecha**: en su casilla va `riego.disparador` ("comprobar que los 2 cm de arriba están secos"), no una cuenta atrás — un intervalo sin día de partida no produce vencimiento, y el disparador real del riego no es el calendario sino el sustrato. Descartada la fecha de ancla **para el riego**: daría un vencimiento aritméticamente impecable y agronómicamente falso.
- [ux-lead] Principio general que sale de ahí: **donde el disparador real sea una observación y no el calendario, la interfaz pide mirar, no manda hacer.** «Una web que dice comprueba enseña a cuidar plantas; una que dice riega hoy solo da órdenes, y a los cuatro días se equivoca» (`botanist`).
- [ux-lead] La única tarea `vencida` **no comparte registro visual con la única `critica`** — son plantas distintas (begonia y helecho) y por tanto ejes independientes: `critica` en rojo invertido, `vencida` en ocre con el rótulo `VA TARDE`, y las otras catorce con fecha **sin distintivo**. Misma economía que hace que las cuatro sanas no lleven insignia.
- [botanist/ux-lead] El rótulo es `VA TARDE` y nunca "incumplido" — `vencida` significa "tiene fecha de referencia y ya pasó", no "alguien lo ha hecho mal", y la fecha es criterio de `botanist` y no norma de RHS: acusar al lector de una tarea que nadie le había dicho es la forma más rápida de que cierre la página.
- [botanist/ux-lead] **La condición gana al mes**: una tarea `condicionada` no aparece nunca en "hoy" aunque el calendario cuadre — abonar un helecho sin frondes le quema las raíces, así que no sería inexacto, sería consejo dañino.
- [ux-lead] Los plazos relativos se pintan relativos y con su origen ("tres semanas después de cambiarla de maceta"), nunca como fecha — la revisión de la begonia cuenta desde el trasplante porque el trasplante es lo que se mide.
- [ux-lead] `localStorage` baja de requisito a mejora opcional — con los tipos de tarea de `botanist` la franja es correcta sin persistencia ninguna.

## Duodécima vuelta — la matriz de validez por superficie

- [ux-lead] `css/tokens.css` incorpora una **matriz de qué token vale sobre qué superficie**, con los ratios calculados y "—" donde no se puede usar — dos fallos reales del proyecto (el enlace azul y el borde del buscador, los dos sobre la teja) fueron el mismo error: un token medido contra un fondo y usado contra otro. **Ningún checker lo detecta**, porque el contraste solo se mide en el navegador con el fondo real compuesto, así que la única defensa posible es declararlo donde se elige el color. Idea del lead.
- [ux-lead] `--color-borde` baja de `#918A7B` a `#8A8375` — al construir la matriz salió que solo lo había medido contra `--color-etiqueta` (3,28) y que sobre las demás superficies claras se quedaba entre 2,87 y 3,08: **suspendía en la superficie hundida, que es justo donde vive el bloque de resúmenes**. Ahora el peor caso es 3,15. Nadie lo había levantado; lo destapó la propia matriz.
- [ux-lead] Regla escrita en la matriz: **un token no hereda su ratio**. Si hace falta un color sobre una superficie nueva no se deduce de la tabla, se mide y se añade una fila.

## Decimotercera vuelta — el brief se sincroniza con el JSON

- [ux-lead] `--texto-nombre` **se queda en 2rem**, pero por criterio tipográfico y no por presión de QA: `qa-visual` midió cero desbordamiento a 320 px y el culpable era el `min-width: auto` de los hijos de grid, ya resuelto. A 36 px el nombre más largo ocupaba el **98,5 % del renglón útil**, y eso no es un margen, es suerte; a 32 px ocupa el 87 % y el techo del clamp no se toca, así que en escritorio no cambia nada.
- [ux-lead] `hyphens: auto` con `lang="es"` se queda aunque hoy no haga falta — es la red que impide que un nombre científico largo rompa la rejilla mañana.
- [ux-lead] **Todas las rutas de campo del brief se corrigen contra el JSON real** (`estado.*` → `estados[].*`, `profundidad_seco_cm` → `profundidad_cm`, `nivel_recibido_estimado` → `nivel_actual`, `orientacion_ventana` → `orientacion`) — `builder` maqueta leyendo el brief, así que una ruta vieja aquí es un `TypeError` silencioso allí: exactamente lo que pasó con `p.medidas.*` y costó los cuatro diagramas.
- [ux-lead] `estados` es un array: el distintivo toma **el peor** de los estados y el bloque `QUÉ LE PASA` se repite por estado con su `titulo` — descartado diseñar para el caso de hoy (todas tienen uno): romperse con dos es la clase de deuda que se paga cuando nadie mira.
- [ux-lead] `revisar_fecha` **no sustituye** a `revisar_en`: la fecha va a la franja `HOY` y la prosa al expediente, porque la prosa describe **dos formas distintas de fracasar y cómo distinguirlas**, y eso no lo reemplaza un `2026-09-01`.
- [ux-lead] `tareas[].prioridad` **ordena pero no se pinta** — no es una cuarta escala de urgencia; la única jerarquía cromática de tareas sigue siendo `VA TARDE`.
- [qa-visual] El riesgo de la teja **queda validado a 320 px**, y por un motivo que yo no había previsto: la franja blanca a sangre corta el terracota justo donde empezaba a pesar. No hace falta apretar `--hueco-rejilla`.

## Decimocuarta vuelta — el fallo peor del proyecto, y en la planta que se muere

- [ux-lead] **El bloque de causas de la ficha crítica estaba pintado sobre `--color-alerta` sólido con `--texto-secundario` encima: 1,19:1.** Texto prácticamente invisible, en la única planta que se está muriendo. La regla ya estaba escrita en la matriz —sobre el rojo sólido solo vale `--color-etiqueta` (7,43); ni siquiera `--color-tinta`, que da 1,74— pero estaba escrita y no era fácil de obedecer.
- [ux-lead] Se añaden `--fondo-critica` (relleno pálido, para el **bloque**) y `--fondo-distintivo-critica` (rojo sólido, **solo** para el chip) — descartado dejarlo en una regla escrita: **la inversión es para el distintivo, nunca para un contenedor de texto**, y la forma de que eso se cumpla es que la composición correcta sea la fácil de escribir, no la advertida.
- [ux-lead] **Los bordes se parten en dos tokens por nombre**: `--color-borde-control` (≥3:1 obligatorio sobre cualquier superficie) y `--color-separador` (sin umbral, nunca delimita algo pulsable) — descartado seguir con `--color-borde` / `--color-borde-sutil`, que son nombres que invitan al error: en CSS los dos se escriben `border` y siempre se acaba cogiendo el más discreto porque queda bonito. `--color-separador` se lee mal en un botón, y ése es el punto.
- [ux-lead] Van cuatro fallos del mismo error con distinto disfraz —enlace azul (1,44), borde del buscador (1,92), borde en superficie hundida (2,87) y los siete tiradores de despegar (1,49)—. El cuarto es el peor porque **el tirador es el gesto que sostiene la signature**.
- [ux-lead] Alias `--color-borde` y `--color-borde-sutil` como **DEPRECATED temporales**: `app.css` no es mío y no se deja el repo roto entre turnos. Con aviso explícito de que el mapeo **no es mecánico** —los siete tiradores van a `--color-borde-control`, no a `--color-separador`—, que es justo el criterio que los nombres nuevos vienen a forzar.
- [ux-lead] Tokens de diana: `--diana-min` (24 px, el mínimo de WCAG 2.5.8 AA), `--diana-comoda` (44 px) y `--espacio-diana` — **el área pulsable se crece con `padding`, nunca con `font-size`**: la microtipografía de las citas es una decisión de diseño y el tamaño de diana es de ergonomía, y son independientes.

- [ux-lead] **El bloque `QUÉ LE PASA` de la crítica deja de ir invertido**: pálido con tinta normal, y solo el chip y el filete en rojo sólido — y ya no es por contraste, que `builder` cerró enumerando catorce selectores. Es por tres cosas: **(a)** invertir un contenedor obliga a enumerar cada descendiente y su propio comentario lo dice ("cualquier color que se añada aquí tiene que entrar en esta lista"), que es la definición de una deuda y **ya falló una vez**; **(b)** sobre el rojo sólido **ninguno** de los seis tokens de texto del sistema se lee —1,17 a 1,74— y sobre el pálido pasan los cinco sin enumerar nada; **(c)** un bloque rojo de miles de píxeles gasta justo lo que el sistema declara escaso, y anula la decisión que hacía visible el filete del helecho desde el otro lado del monitor.
- [ux-lead] Regla escrita en `tokens.css`: **no se invierte un contenedor de texto, solo un distintivo.** Un contenedor donde solo un color del sistema es válido no es un contenedor: es una excepción con forma de bloque.

- [builder/ux-lead] **Se corrige mi regla de "nada se pliega", que era demasiado gruesa**: no se pliegan ni las afirmaciones (`causa__resumen`) ni los límites de lo que sabemos (`LO QUE LA FOTO NO DICE`) ni el patrón de reconocimiento —que es lo que se necesita delante de la planta—, pero **el razonamiento sí** puede ir tras un "Por qué". `builder` lo repartió así sin que se lo dijeran y su versión es la correcta: esconder "esto no lo podemos afirmar" decide por el lector que no le interesa; esconder el porqué tras un rótulo que dice "Por qué" no esconde nada, lo ordena.
- [ux-lead] Borrados los alias `--color-borde` y `--color-borde-sutil`: `builder` migró **uso por uso** (5 a control, 10 a separador) y quedan cero referencias.

## Decimoquinta vuelta — revisión del expediente: el objetivo no era mío y el peor caso no era el helecho

- [ux-lead] **Se retira el "objetivo de 2.400 px de `ux-lead`": nunca fue una decisión mía.** `git grep` y `git log -S` sobre todo el historial lo sitúan **solo** en `docs/qa/informe-2.md`, atribuido a mí — no está en `brief.md`, ni en este fichero, ni en ningún commit. Descartado defenderlo o justificar el desvío, que es lo que se me encargaba: llevaba dos pasadas de QA sosteniendo un ALTA y no tenía procedencia. Una cita se había vuelto fuente.
- [ux-lead] Sustituido por **dos objetivos con derivación**: (a) **ocupación** — ninguna ficha con más del 20 % de sus bandas de 100 px parando antes del 62 % del ancho (hoy: 73–87 % en las siete); (b) **altura por severidad** — ≤3 pantallas (2.700 px) en `critica`/`atencion`, ≤2 (1.800 px) en `sana`, a 1280×900. Descartado un número único para las siete: obliga a que una planta sin problema y una que se muere quepan en lo mismo, y la única forma de lograrlo es recortarle contenido a la que lo necesita o inventárselo a la que no.
- [ux-lead] **La altura no la manda la severidad, y eso invalida mi propio razonamiento**: la begonia (`atencion`, 4.727 px) y el ficus (`sana`, 4.109) son más largos que el helecho crítico (3.718). El brief daba por hecho que la crítica era la más larga porque lleva el diagnóstico entero. Medido en las siete, falso.
- [ux-lead] **En `sana` el rótulo `CAUSAS PROBABLES` se cambia por mejoras opcionales** — descartado dejarlo: el poto sano carga 5 causas y 1.955 caracteres, el mismo peso que el helecho que se muere (5 y 1.970). El contenido de `botanist` es correcto y útil; lo que está mal es preguntarle la causa de un problema a una planta que no lo tiene. **Un rótulo que obliga a escribir contenido para justificarse no es decoración: la decoración ocupa sitio, esto lo fabrica.** Y con el nombre correcto ya puede plegarse sin romper mi regla, porque una propuesta no es una afirmación sobre un problema ni un límite de lo que sabemos.
- [ux-lead] **Se borra el `diagrama--recuperacion`** — descartado arreglarlo con el eje logarítmico que yo mismo había especificado. Sus seis círculos están en `cx` 10/30/50/70/90/110, espaciado regular, para pasos que son *inmediato, inmediato, esta semana, 3 semanas, 2-3 meses* y *cuando haya una fronde adulta*: el eje codifica el índice 1…6 y va rotulado con dos fechas (`hoy`, `01/09/2026`), así que **afirma que el paso 6 cae el 1 de septiembre, que es el `revisar_fecha` y no es ningún paso**. Sobre el dato real el eje honesto no es construible: dos pasos en el instante 0 no tienen posición y uno sin fecha tampoco. La lista numerada de debajo ya rotula cada paso con su horizonte en palabras, que es más preciso que un eje. Aplicada mi regla eliminatoria, se borra y **se gana**: desaparece una afirmación falsa.
- [ux-lead] Los otros tres diagramas **se quedan, comprobados uno a uno**: `--riego` tiene marcas en 20/21,96/27,64/36,49/47,64/60/72,36 (escala real), `--luz` usa bandas regulares y es correcto porque son categorías ordinales y no tiempo, `--temp` dibuja rango y sub-rango sobre un eje 0–40 °C rotulado. Solo sobraba el cuarto — descartado condenar los cuatro por el fallo de uno.
- [ux-lead] **No se levanta `PATRÓN PARA RECONOCERLA` como fallo**: el helecho trae `patron: null` en sus cinco causas y es correcto —son hechos históricos, no cuadros que se distingan mirando—; en el resto está, 21 de 35. El render acierta al no dibujar un bloque vacío. Iba a reportarlo y lo comprobé antes.
- [ux-lead] **Ni un token nuevo para las dos columnas**: fondo `--color-etiqueta`, filete `--color-separador` (no delimita nada pulsable). La matriz de validez no necesita fila **hoy**; si la columna sticky acaba con fondo propio, se mide antes de usarla — un token no hereda su ratio.

## Decimosexta vuelta — vuelta de ambición visual, sin importar un look

- [ux-lead] **No se parte de webs de referencia** — descartado buscar sitios premiados y basarse en uno: de esa búsqueda salen tres direcciones, y **la primera (crema, serif de alto contraste, acento terracota) ya es casi esta web por accidente**. Estamos a un paso del promedio y lo único que nos separa es la procedencia: el terracota es el plástico medido del tiesto del coleo y el blanco el satinado de la pegatina de Móstoles. Una referencia externa vale para resolver una técnica, nunca para adoptar un look.
- [ux-lead] **El parte del día pasa de cintillo a hero, a escala de display** — descartado añadir un elemento nuevo a la portada: el defecto no es que falte nada, es que **nada en la primera pantalla es grande** (el `h1` compite en peso con el título de la tarjeta de abajo) y los primeros 360 px son seis filas de chrome pequeño sobre terracota. La tesis de la página era lo más pequeño de la pantalla.
- [ux-lead] **Tamaño y nivel de encabezado son ejes independientes, y se escribe explícito**: el `h1` se pinta como eyebrow pero **sigue siendo `<h1>`**, y el veredicto del día no se promociona a encabezado por ser lo mayor — descartado dejarlo implícito, que es como se mezclan y como se rompe el esquema de encabezados.
- [ux-lead] **El hero se diseña para el día bueno**: forma fija (veredicto + hasta tres entradas) y contenido variable, de modo que `LAS 7 ESTÁN BIEN` se componga con la misma confianza que `3 DE 7 PIDEN MIRADA` — descartado un hero que solo funcione habiendo alarma, porque **ese día es el estado deseado del proyecto, no un caso borde**. Prohibido por escrito inventar urgencia para conservar la tensión: sin rachas, sin porcentaje, sin check de "todo en orden".
- [ux-lead] **El día bueno no estrena color**: se compone en `--color-tinta` — descartado un verde de celebración, coherente con que en este sistema no exista color para severidad `sana`. Un token nuevo para decir lo que la tipografía ya dice.
- [ux-lead] **La única excepción a la regla eliminatoria se gasta en el revelado del cintillo al cargar, como se imprime una etiqueta térmica**: una banda que resuelve de arriba abajo — descartado el cursor que teclea letra a letra, que es el tic que delata lo generado **y que además no es como imprime una etiqueta térmica**. Un disparo, sobre texto ya en el DOM, sin desplazamiento de layout, y con `reduce` aparece entera.
- [ux-lead] **Las fotos no llevan ningún filtro** — descartados duotono, viñeta, grado cálido y fondo difuminado: **filtrar una foto de diagnóstico corrompe la prueba** y en la única planta que se muere la haría parecer mejor de lo que está. La foto nocturna de móvil no es un defecto que disimular, es la condición real en que se documenta esta casa.
- [ux-lead] En su lugar, **la foto declara qué no se puede juzgar en ella, con su `fecha_foto`**: el dato ya estaba escrito por `botanist` y enterrado como una observación entre trece. Pasa de documentación a prueba fechada. **En banda de superficie sólida pegada a la imagen, nunca sobre los píxeles** — descartado el texto encima: es contraste no medible (ya hay 24 nodos así) y sobre la foto nocturna del helecho sería ilegible.
- [ux-lead] **El despegue bisagra por un canto y el expediente se descubre por debajo** — descartado seguir con el fade actual: hoy la `.etiqueta` **nunca se mueve** (solo se profundiza la sombra y el panel hace `scaleY(0.97)` con 8 px de `translateY`), y **`--ease-troquel`, cuyo comentario dice "el rebote mínimo del despegue", está gastado en una opacidad**. El sistema documentaba un gesto que nadie había construido; no hay que inventar nada, hay que cumplir lo que los tokens prometen.
- [ux-lead] **`overflow: hidden` en `.etiqueta` (app.css:421) rompe el `position: sticky` de la columna de acción del expediente. Arreglo: `overflow: clip`.** Medido con una sonda sticky scrolleando 800 px: con `hidden` la sonda se va a −775 px, con `clip` y con `visible` se queda en 16. `clip` recorta igual —la esquina troquelada de 3 px y la signature se mantienen— pero no crea contenedor de scroll. Descartado quitar el recorte, que se llevaría el canto de la pegatina.
- [ux-lead] **Descartada la explicación de que el `transform` de la bisagra rompería el sticky**: medido, con `transform: rotateX(2deg)` en `.etiqueta` la sonda sigue pegando (17 px). Un `transform` crea containing block para `fixed`/`absolute` pero **no** contenedor de scroll. La preocupación era acertada y la atribución no — y la mitigación propuesta (mover el `transform` a la etiqueta) no habría arreglado nada, porque la columna sticky es descendiente de `.etiqueta`.

## Decimoséptima vuelta — el tope de altura era mi 2.400, y dos correcciones a mí mismo

- [ux-lead] **Retirado el tope de altura en píxeles (2.700 / 1.800). Era mi 2.400 en versión suave** — descartado recalibrarlo: lo derivé de "dos pantallas", un número redondo de viewports, no del contenido, que es exactamente el defecto que le reproché al 2.400 y en el mismo documento. Medido: una ficha sana borrando el bloque de causas **entero** seguiría en ~2.527 px contra un tope de 1.800, así que no se alcanzaba maquetando, solo borrando foto, diagramas, límites o fuentes. **La altura siempre fue un proxy de "muro de texto"**; ahora que la propiedad real se puede medir, el proxy se retira en vez de buscarle un número que cuadre.
- [ux-lead] **Objetivo nuevo que sí sale del contenido: ninguna carrera de más de 600 px sin un ancla de navegación** (rótulo, entrada de índice o diagrama) — descartado el alto total como criterio de suspenso: explica por qué una ficha de 2.886 px con un rótulo cada 400 se lee bien y una de 1.800 de un solo bloque no. La ocupación ≤20 % se queda (cumplida: 9 %, viniendo de 73–87 %).
- [ux-lead] **La altura en píxeles pasa de objetivo a observación**: se mide y se apunta, pero no suspende — un objetivo manda cortar, una observación manda mirar. Y descartada explícitamente la opción "el tope está bien y el contenido sobra": **el contenido no se recorta para cuadrar una cifra mía.**
- [ux-lead] **Corregido: lo retirado era `diagrama--recuperacion`, no "el diagrama de cronología"** — les puse el mismo nombre a dos diagramas distintos. El de cronología **se queda y es sólido**: Poto >20 años contra cuatro que llegaron hoy es un rango real de cuatro órdenes de magnitud medido de `fecha_llegada`, o sea el caso en que un eje logarítmico es la representación correcta; y `builder` resolvió con una fila por planta la objeción de los marcadores apilados en el origen que yo había puesto al otro. Uno tenía un eje que mentía y el otro tiene un eje que dice la verdad sobre un dato que si no no se ve.
- [botanist/ux-lead] **Aceptada la clave `tipo` (`causa|afirmacion|riesgo|mejora|aclaracion`, ausente = `causa`) en lugar de mi rótulo «mejoras opcionales»** — descartado tanto el rótulo anterior como el mío: **el desajuste es por ítem, no por severidad**, así que ningún rótulo de bloque podía encajar. Mi versión misfilaba el ítem más grave de cada ficha sana —«podredumbre de raíz» como "mejora opcional", que es la forma nº1 de matar un poto— y encima lo habría plegado. `afirmacion`, `riesgo` y `aclaracion` no se pliegan; `mejora` sí. Rótulo interino hasta que exista: **«lo que hay que saber»**.
- [ux-lead] Van tres veces que una regla mía sale demasiado gruesa y un teammate la afina con el contenido delante (`builder` con el plegado, `botanist` con esto, dos veces). **Una regla de presentación escrita sin los 35 ítems delante misfila el caso grave, porque el caso grave es siempre el raro.**
- [ux-lead] **La casa en invierno se dibuja como banda (21–24) y el verano sigue siendo marcador de tope (28)** — descartado unificar las dos formas: convertir el invierno en punto inventaría el 22,5 que `botanist` se negó a inventar, y convertir el verano en banda dibujaría un intervalo donde solo hay un tope. **La forma codifica la clase de dato**; que dos datos se dibujen distinto no es inconsistencia, es la información. Con esto el diagrama gana su motivo: en tres de las siete la casa cae fuera de lo que la especie quiere, y el poto es el contraejemplo.
- [ux-lead] **Entrada por síntoma: entra, y va en el buscador, no en la portada** — descartado darle una región propia en la portada, que competiría con el hero. No es una función nueva que justificar: el placeholder ya dice "planta, sala o **síntoma**" y no se cumple, así que **es una promesa que miente**. Y **el índice se genera de `senales`/`patron`, no de una taxonomía fija** — descartada la lista cerrada de síntomas: generándolo, cada entrada tiene por construcción al menos una planta y desaparece el problema de qué decir de un síntoma que hoy nadie tiene.

## Decimoctava vuelta — la métrica con su algoritmo, y el defecto simétrico

- [ux-lead] **La métrica de ocupación se escribe con su algoritmo completo** (referencia = contenedor del expediente de una ficha abierta, W = caja de contenido, bandas de 100 px, cuentan solo elementos con texto propio o `img`/`svg`, perfil = mayor `right` por banda, banda corta = perfil > 0 y < 0,62·W, **las bandas vacías se excluyen del numerador y del denominador**) — descartado dejarla descrita por su nombre: una métrica sin algoritmo escrito es, dentro de dos informes, lo que cada uno recuerde. Es la lección del 2.400 aplicada antes de que hiciera falta. La discrepancia con `qa-visual` (68–76 % contra 73–87 %) era de referencia: yo medí `section.estado` y él la ficha entera; la correcta es el expediente.
- [ux-lead] **Escrito que el `0,62` NO está derivado**: es "una medida de lectura cómoda (557–640 px, el 50–57 % de un contenedor de 1.116) más un margen". La derivación honesta sería *"¿cabía otra columna en lo que sobra?"* — columna mínima 13 rem más hueco, umbral cerca del 78 % —. **Se queda en 62 % a propósito**: el 78 % suspendería párrafos finales de sección que legítimamente no llenan el ancho, y se prefiere una métrica que solo cace el defecto gordo. Descartado dejar el número sin su procedencia, que es exactamente cómo nació el 2.400.
- [ux-lead] **Toda ficha `sana` lleva al menos una `afirmacion`, primera y sin plegar** — descartado tratarlo como el caso puntual del ficus que levantó `botanist`: comprobado en el JSON, **dos de las cuatro sanas** (ficus y coleo pequeño) se quedaban con solo riesgos y aclaraciones. Y no es presentación: **es el defecto simétrico del que acabábamos de arreglar.** Una ficha sana que solo enseña tres riesgos **dice algo falso** —que la planta está en apuros— igual que «causas probables» decía que tenía un problema; el rótulo manufacturaba contenido y esto manufactura alarma. En `critica`/`atencion` no se exige: forzar que se afirme que una planta enferma está sana sería el mismo error al revés.
- [ux-lead] La afirmación **se cambia de sitio, no se inventa**: ya está escrita dentro del `detalle` de otro ítem (el ficus tiene su «está sano y no necesita explicación: necesita no romperse» enterrado en el ítem de procedencia). Ésa era la duda razonable de `botanist` —no añadir contenido por motivos de presentación— y se resuelve porque no hay adición, hay refiliado.
- [ux-lead/botanist] Confirmado con los números que el reparto por ítem era el correcto: **«mejoras opcionales» iba a rotular 17 ítems y describe exactamente 1**, y el rótulo de bloque estaba mal en **5 de las 7 plantas**, no en las 4 sanas. Mi corrección no misfilaba una excepción: misfilaba 16 de 17.

## Decimonovena vuelta — el rótulo del `patron`, y una afirmación que se puede desmentir

- [ux-lead] **El rótulo del `patron` varía por `tipo`**: `causa` → `PATRÓN PARA RECONOCERLA`, `riesgo` → `SEÑAL DE QUE ESTÁ PASANDO`, `afirmacion` → `QUÉ LO DESMENTIRÍA` — descartado mantener el rótulo único que escribí: sobre una afirmación no significa nada, porque el `patron` que `botanist` ha escrito ahí (*"el día que la hoja nueva salga más verde y con el borde más estrecho, le falta luz"*) **es una condición de falsación, no una forma de reconocer algo**. El rótulo se escribió cuando el campo solo vivía en `causa`.
- [ux-lead] Criterio general, tercera aplicación del día tras `tipo` y las dos formas del diagrama térmico: **cuando un contenedor tiene que adivinar de qué clase es su contenido, la solución no es un rótulo más listo, es etiquetar la clase en el dato.** Un rótulo por clase, no un rótulo por bloque.
- [ux-lead/botanist] **Criterio de contenido nuevo: una `afirmacion` que trae su propia condición de falsación es más fuerte que una que no**, y donde pueda llevarla, la lleva — descartado dejar las afirmaciones como aserciones sin comprobación. «Está sano» es una opinión; «está sano, y el día que la hoja nueva salga más verde es que me equivoqué» se puede comprobar. Es la disciplina de honestidad del proyecto aplicada a las buenas noticias, que era **el último sitio donde quedaba hueco para el optimismo**: los huecos ya se marcaban y las fuentes ya se citaban, pero una afirmación positiva no tenía forma de desmentirse.
- [ux-lead] Anotado que esta corrección **añade** dos ítems y sube las fichas sanas: con el tope de 1.800 px vivo, la mejor decisión de contenido del día habría competido con una métrica mía. Es la demostración de para qué servía distinguir objetivo de observación — **un objetivo manda cortar, y aquí cortar era lo malo.**

## Accesibilidad

- [ux-lead] Anillo de foco doble (2 px Tinta + halo 3 px Etiqueta) en un solo token — descartado el anillo de un color, porque ningún color de la paleta pasa 3:1 contra el blanco de la ficha y contra la teja del fondo a la vez.
- [ux-lead] Sobre el fondo teja solo se escribe en blanco; Tinta sobre Maceta queda prohibido y anotado en `tokens.css` — el par da 2,05:1 y habría entrado por descuido en la primera cabecera que alguien escribiera.
- [ux-lead] Los contrastes se calcularon con un script antes de fijar los hex, no después — descartado ajustar la paleta en QA, que siempre acaba en "casi pasa" y en repintar media web.
